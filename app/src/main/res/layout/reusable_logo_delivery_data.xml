<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <TextView
        android:id="@+id/del_day_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="@dimen/space_between_views"
        android:textSize="@dimen/nav_drawer_header"
        android:text="@string/DELIVERY_DAY"
        android:layout_centerVertical="true"
        android:textStyle="bold"
        android:textColor="@color/colorDarkGray"
        android:layout_toRightOf="@+id/product_company_logo"
        android:layout_toEndOf="@+id/product_company_logo" />

    <ImageView
        android:src="@drawable/ic_action_shipping"
        android:layout_width="@dimen/nav_drawer_icon_dim"
        android:layout_height="@dimen/nav_drawer_icon_dim"
        android:id="@+id/product_company_logo"
        android:layout_marginRight="@dimen/five_dp"
        android:layout_centerVertical="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:scaleType="fitXY"
        android:adjustViewBounds="true"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/del_day_date"
        android:textColor="@color/colorBlack"
        android:text="Week, XX Mon YYYY"
        android:lines="1"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@+id/del_day_text"
        android:layout_toEndOf="@+id/del_day_text" />

</RelativeLayout>